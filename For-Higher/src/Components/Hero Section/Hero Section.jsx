import React from 'react';
import './Hero Section.css';

const HeroSection = () => {
    return (
        <section className="hero-section">
            <h2>Welcome to For Higher</h2>
            <p>Your platform for connecting to applicants, for higher.</p>
        </section>
    );
};

export default HeroSection;
